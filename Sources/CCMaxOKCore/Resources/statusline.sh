#!/bin/bash
# Auto-generated at runtime by StatuslineSetup.swift with absolute paths.
# DO NOT copy this file manually — the deployed version uses the resolved path.
cat /dev/stdin > ~/.claude/ccmaxok/live-status.json
