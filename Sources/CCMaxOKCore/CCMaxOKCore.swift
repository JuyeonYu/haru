import os

/// CCMaxOKCore — Core logic for Claude Code usage monitoring.
public enum CCMaxOKCore {
    public static let version = "0.1.0"
    public static let logger = Logger(subsystem: "com.ccmaxok", category: "core")
}
